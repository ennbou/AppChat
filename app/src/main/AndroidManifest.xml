<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appchat">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".compte.newPassword"
            android:parentActivityName=".compte.Login"/>
        <activity android:name=".message.Messages" />
        <activity android:name=".principalAcitivity.MainActivity" />
        <activity android:name=".compte.Login" />
        <activity android:name=".start.Bar_layout" />
        <activity
            android:name=".compte.Inscription"
            android:parentActivityName=".compte.Login" />
        <activity android:name=".start.Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>